version: "3"

services:

  test:
    container_name: test_container
    hostname: test_container
    image: test_container
    build: .
    networks:
      - inception

networks:
  inception:
    external: true
